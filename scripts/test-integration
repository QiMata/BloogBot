#!/usr/bin/env bash
set -euo pipefail
# POSIX shim -> PowerShell implementation. Real logic lives in test-integration.ps1.
exec pwsh -NoProfile -File "$(cd "$(dirname "$0")" && pwd)/test-integration.ps1" "$@"
