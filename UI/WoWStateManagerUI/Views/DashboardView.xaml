<UserControl x:Class="WoWStateManagerUI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WoWStateManagerUI.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:DashboardViewModel}"
             mc:Ignorable="d" d:DesignWidth="1080" d:DesignHeight="600">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Realm/World/Pop summary + StateManager launch controls -->
        <Grid Grid.Row="0" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Realm:" VerticalAlignment="Center" HorizontalContentAlignment="Right"/>
            <Label Grid.Column="1" Content="{Binding RealmState}" d:Content="UP" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <Label Grid.Column="2" Content="World:" VerticalAlignment="Center" HorizontalContentAlignment="Right"/>
            <Label Grid.Column="3" Content="{Binding WorldState}" d:Content="UP" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <Label Grid.Column="4" Content="Pop.:" VerticalAlignment="Center" HorizontalContentAlignment="Right"/>
            <Label Grid.Column="5" Content="{Binding TotalPopulation}" d:Content="3000" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0"/>

            <Button Grid.Column="7" Margin="4,0" Padding="8,4" Content="Exe..." Command="{Binding BrowseExeCommand}" ToolTip="Select StateManager executable"/>
            <Button Grid.Column="8" Margin="4,0" Padding="8,4" Content="Launch SM" Command="{Binding LaunchStateManagerCommand}"/>
            <Button Grid.Column="9" Margin="4,0" Padding="8,4" Content="Stop SM" Command="{Binding StopStateManagerCommand}"/>
        </Grid>

        <!-- Row 1: Status message + selected exe path (read-only display) -->
        <Grid Grid.Row="1" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" Foreground="DarkSlateGray" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="1" Text="{Binding StateManagerExePath}" Foreground="DarkSlateGray" VerticalAlignment="Center" FontStyle="Italic" Margin="8,0,0,0"/>
        </Grid>

        <!-- Row 2: Live bot grid (left) + selected bot detail (right) -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" ItemsSource="{Binding Bots}" SelectedItem="{Binding SelectedBot}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                      SelectionMode="Single" GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column" BorderThickness="1" BorderBrush="Gray">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Account" Binding="{Binding AccountName}" Width="100"/>
                    <DataGridTextColumn Header="Character" Binding="{Binding CharacterName}" Width="110"/>
                    <DataGridTextColumn Header="Race" Binding="{Binding RaceDisplay}" Width="75"/>
                    <DataGridTextColumn Header="Class" Binding="{Binding ClassDisplay}" Width="60"/>
                    <DataGridTextColumn Header="Lvl" Binding="{Binding Level}" Width="40"/>
                    <DataGridTextColumn Header="Area" Binding="{Binding AreaDisplay}" Width="120"/>
                    <DataGridTextColumn Header="Action" Binding="{Binding CurrentAction}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="8">
                        <TextBlock Text="Selected Bot" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <Grid Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Column="0" Grid.Row="0" Text="Account:" Foreground="DarkSlateGray"/>
                            <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding SelectedDetail.Snapshot.AccountName}"/>
                            <TextBlock Grid.Column="0" Grid.Row="1" Text="Character:" Foreground="DarkSlateGray"/>
                            <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding SelectedDetail.Snapshot.CharacterName}"/>
                            <TextBlock Grid.Column="0" Grid.Row="2" Text="Race:" Foreground="DarkSlateGray"/>
                            <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding SelectedDetail.Snapshot.RaceDisplay}"/>
                            <TextBlock Grid.Column="0" Grid.Row="3" Text="Class:" Foreground="DarkSlateGray"/>
                            <TextBlock Grid.Column="1" Grid.Row="3" Text="{Binding SelectedDetail.Snapshot.ClassDisplay}"/>
                            <TextBlock Grid.Column="0" Grid.Row="4" Text="Level:" Foreground="DarkSlateGray"/>
                            <TextBlock Grid.Column="1" Grid.Row="4" Text="{Binding SelectedDetail.Snapshot.Level}"/>
                            <TextBlock Grid.Column="0" Grid.Row="5" Text="Area:" Foreground="DarkSlateGray"/>
                            <TextBlock Grid.Column="1" Grid.Row="5" Text="{Binding SelectedDetail.Snapshot.AreaDisplay}"/>
                            <TextBlock Grid.Column="0" Grid.Row="6" Text="Position:" Foreground="DarkSlateGray"/>
                            <TextBlock Grid.Column="1" Grid.Row="6" Text="{Binding SelectedDetail.Snapshot.Position}"/>
                            <TextBlock Grid.Column="0" Grid.Row="7" Text="Health:" Foreground="DarkSlateGray"/>
                            <StackPanel Grid.Column="1" Grid.Row="7" Orientation="Horizontal">
                                <TextBlock Text="{Binding SelectedDetail.Snapshot.Health}"/>
                                <TextBlock Text=" / "/>
                                <TextBlock Text="{Binding SelectedDetail.Snapshot.MaxHealth}"/>
                            </StackPanel>
                            <TextBlock Grid.Column="0" Grid.Row="8" Text="Travel:" Foreground="DarkSlateGray"/>
                            <TextBlock Grid.Column="1" Grid.Row="8" Text="{Binding SelectedDetail.Travel}" TextWrapping="Wrap"/>
                            <TextBlock Grid.Column="0" Grid.Row="9" Text="Loadout:" Foreground="DarkSlateGray"/>
                            <TextBlock Grid.Column="1" Grid.Row="9" Text="{Binding SelectedDetail.LoadoutStatus}"/>
                            <TextBlock Grid.Column="0" Grid.Row="10" Text="Action:" Foreground="DarkSlateGray"/>
                            <TextBlock Grid.Column="1" Grid.Row="10" Text="{Binding SelectedDetail.Snapshot.CurrentAction}"/>
                            <TextBlock Grid.Column="0" Grid.Row="11" Text="Spells:" Foreground="DarkSlateGray"/>
                            <TextBlock Grid.Column="1" Grid.Row="11" Text="{Binding SelectedDetail.SpellCount}"/>
                        </Grid>
                        <Separator Margin="0,8"/>
                        <TextBlock Text="Stats" FontWeight="SemiBold" Margin="0,0,0,4"
                                   Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}"/>
                        <DataGrid ItemsSource="{Binding SelectedDetail.Stats}" AutoGenerateColumns="False"
                                  IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column"
                                  GridLinesVisibility="Horizontal" BorderThickness="0"
                                  Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <TextBlock Text="Select a bot to view details." FontStyle="Italic" Foreground="DarkSlateGray"
                                   Visibility="{Binding HasNoSelection, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
