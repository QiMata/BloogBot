<Window x:Class="WoWStateManagerUI.Views.SearchPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WoWStateManagerUI.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:SearchPickerViewModel}"
        mc:Ignorable="d" Title="{Binding Title}" Height="520" Width="720"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: search box -->
        <Grid Grid.Row="0" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Search:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Grid.Column="1" Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Padding="4,2">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
            <Button Grid.Column="2" Content="Find" Command="{Binding SearchCommand}" Padding="12,4" Margin="6,0,0,0"/>
        </Grid>

        <!-- Row 1: results -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Results}"
                  SelectedItem="{Binding SelectedResult}"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  SelectionMode="Single" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" BorderThickness="1" BorderBrush="Gray"
                  MouseDoubleClick="OnResultsDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Info" Binding="{Binding Extra}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Row 2: status -->
        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="DarkSlateGray" Margin="0,6,0,0"/>

        <!-- Row 3: OK / Cancel -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="OK" Click="OnOk" Padding="16,4" Margin="0,0,6,0" IsDefault="True"/>
            <Button Content="Cancel" Click="OnCancel" Padding="16,4" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
