<UserControl x:Class="WoWStateManagerUI.Views.ServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WoWStateManagerUI.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:ServicesViewModel}"
             mc:Ignorable="d" d:DesignWidth="1080" d:DesignHeight="560">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="140"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Connect + Filter + Stack operations -->
        <Grid Grid.Row="0" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Connect" Command="{Binding ConnectCommand}" Padding="10,4" Margin="0,0,4,0"/>
            <Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshCommand}" Padding="10,4" Margin="0,0,12,0"/>
            <Label Grid.Column="2" Content="Filter:" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="3" ItemsSource="{Binding ProjectFilters}" SelectedItem="{Binding FilterProject}" Width="100" Margin="4,0,0,0" VerticalContentAlignment="Center"/>

            <!-- WWoW stack operations -->
            <Button Grid.Column="5" Content="Start WWoW Stack" Command="{Binding StartWwowStackCommand}" Padding="8,4" Margin="0,0,4,0"/>
            <Button Grid.Column="6" Content="Restart WWoW Stack" Command="{Binding RestartWwowStackCommand}" Padding="8,4" Margin="0,0,4,0"/>
            <Button Grid.Column="7" Content="Stop WWoW Stack" Command="{Binding StopWwowStackCommand}" Padding="8,4"/>
        </Grid>

        <!-- Row 1: Selected container actions -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,6">
            <TextBlock Text="Selected:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBlock Text="{Binding SelectedName}" VerticalAlignment="Center" Margin="0,0,12,0" MinWidth="120"/>
            <Button Content="Start" Command="{Binding StartCommand}" Padding="10,4" Margin="0,0,4,0"/>
            <Button Content="Stop" Command="{Binding StopCommand}" Padding="10,4" Margin="0,0,4,0"/>
            <Button Content="Restart" Command="{Binding RestartCommand}" Padding="10,4" Margin="0,0,4,0"/>
            <Button Content="View Logs" Command="{Binding ViewLogsCommand}" Padding="10,4" Margin="0,0,4,0"/>
        </StackPanel>

        <!-- Row 2: Container DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding FilteredContainers}" SelectedItem="{Binding SelectedContainer}"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  SelectionMode="Single" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" BorderThickness="1" BorderBrush="Gray"
                  CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Project" Binding="{Binding Project}" Width="75"/>
                <DataGridTextColumn Header="Container" Binding="{Binding Name}" Width="140"/>
                <DataGridTextColumn Header="Image" Binding="{Binding Image}" Width="200"/>
                <DataGridTextColumn Header="State" Binding="{Binding State}" Width="65"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="160"/>
                <DataGridCheckBoxColumn Header="Healthy" Binding="{Binding IsHealthy}" Width="55"/>
                <DataGridTextColumn Header="Ports" Binding="{Binding Ports}" Width="*"/>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding State}" Value="running">
                            <Setter Property="Background" Value="#E8F5E9"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding State}" Value="exited">
                            <Setter Property="Background" Value="#FFEBEE"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Row 3: Log label -->
        <TextBlock Grid.Row="3" Text="Container Logs" FontWeight="SemiBold" Margin="0,6,0,2"/>

        <!-- Row 4: Log output -->
        <Border Grid.Row="4" BorderBrush="Gray" BorderThickness="1">
            <TextBox Text="{Binding LogOutput, Mode=OneWay}" IsReadOnly="True"
                     FontFamily="Consolas" FontSize="10" TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     Background="#1E1E1E" Foreground="#D4D4D4" BorderThickness="0"/>
        </Border>

        <!-- Row 5: Status -->
        <TextBlock Grid.Row="5" Text="{Binding StatusMessage}" Foreground="DarkSlateGray" Margin="0,6,0,0"/>
    </Grid>
</UserControl>
