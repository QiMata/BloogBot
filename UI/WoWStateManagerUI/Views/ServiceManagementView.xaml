<UserControl x:Class="WoWStateManagerUI.Views.ServiceManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WoWStateManagerUI.ViewModels"
             xmlns:views="clr-namespace:WoWStateManagerUI.Views"
             d:DataContext="{d:DesignInstance Type=vm:ServiceManagementViewModel}"
             mc:Ignorable="d" d:DesignWidth="1080" d:DesignHeight="600">
    <TabControl Margin="0">
        <TabItem Header="Containers">
            <views:ServicesView DataContext="{Binding Services}"/>
        </TabItem>
        <TabItem Header="Accounts">
            <views:AccountManagementView DataContext="{Binding Accounts}"/>
        </TabItem>
        <TabItem Header="MaNGOS Console">
            <views:MangosConsoleView DataContext="{Binding MangosConsole}"/>
        </TabItem>
    </TabControl>
</UserControl>
