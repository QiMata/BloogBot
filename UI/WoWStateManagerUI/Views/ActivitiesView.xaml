<UserControl x:Class="WoWStateManagerUI.Views.ActivitiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WoWStateManagerUI.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:ActivitiesViewModel}"
             mc:Ignorable="d" d:DesignWidth="1080" d:DesignHeight="560">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Activities loaded across connected StateManager instances. Auto-refreshes as snapshots arrive."
                   FontStyle="Italic" Foreground="DarkSlateGray" Margin="0,0,0,6"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Activities list -->
            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Activities" FontWeight="SemiBold" Margin="6,4"/>
                    <ListBox ItemsSource="{Binding Activities}" SelectedItem="{Binding SelectedActivity}"
                             DisplayMemberPath="Label" BorderThickness="0"/>
                </DockPanel>
            </Border>

            <!-- Right: Bot snapshot grid for selected activity -->
            <DataGrid Grid.Column="2" ItemsSource="{Binding SelectedActivity.Bots}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                      SelectionMode="Single" GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column" BorderThickness="1" BorderBrush="Gray">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Account" Binding="{Binding AccountName}" Width="100"/>
                    <DataGridTextColumn Header="Character" Binding="{Binding CharacterName}" Width="110"/>
                    <DataGridTextColumn Header="Race" Binding="{Binding RaceDisplay}" Width="80"/>
                    <DataGridTextColumn Header="Class" Binding="{Binding ClassDisplay}" Width="80"/>
                    <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="50"/>
                    <DataGridTextColumn Header="Health" Binding="{Binding Health}" Width="55"/>
                    <DataGridTextColumn Header="Area" Binding="{Binding AreaDisplay}" Width="120"/>
                    <DataGridTextColumn Header="Position" Binding="{Binding Position}" Width="130"/>
                    <DataGridTextColumn Header="Screen" Binding="{Binding ScreenState}" Width="80"/>
                    <DataGridTextColumn Header="State" Binding="{Binding ConnectionState}" Width="90"/>
                    <DataGridTextColumn Header="Action" Binding="{Binding CurrentAction}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="DarkSlateGray" Margin="0,6,0,0"/>
    </Grid>
</UserControl>
