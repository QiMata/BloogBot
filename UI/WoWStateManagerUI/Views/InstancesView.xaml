<UserControl x:Class="WoWStateManagerUI.Views.InstancesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WoWStateManagerUI.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:InstancesViewModel}"
             mc:Ignorable="d" d:DesignWidth="1080" d:DesignHeight="560">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Listener controls -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Label Content="Listen Address:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding ListenAddress, UpdateSourceTrigger=PropertyChanged}" Width="100" Margin="4,2" VerticalAlignment="Center"/>
            <Label Content="Port:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding ListenPort, UpdateSourceTrigger=PropertyChanged}" Width="60" Margin="4,2" VerticalAlignment="Center"/>
            <Button Content="Start Listener" Command="{Binding StartListenerCommand}" Margin="8,0,4,0" Padding="10,4"/>
            <Button Content="Stop Listener" Command="{Binding StopListenerCommand}" Margin="4,0" Padding="10,4"/>
        </StackPanel>

        <!-- Row 1: Main content — instances list + bot details -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Instance list -->
            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Connected Instances" FontWeight="SemiBold" Margin="6,4"/>
                    <ListBox ItemsSource="{Binding Instances}" SelectedItem="{Binding SelectedInstance}"
                             DisplayMemberPath="InstanceId" BorderThickness="0"/>
                </DockPanel>
            </Border>

            <!-- Right: Bot snapshot grid for selected instance -->
            <DataGrid Grid.Column="2" ItemsSource="{Binding SelectedInstance.Bots}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                      SelectionMode="Single" GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column" BorderThickness="1" BorderBrush="Gray">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Account" Binding="{Binding AccountName}" Width="100"/>
                    <DataGridTextColumn Header="Character" Binding="{Binding CharacterName}" Width="100"/>
                    <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="45"/>
                    <DataGridTextColumn Header="Health" Binding="{Binding Health}" Width="55"/>
                    <DataGridTextColumn Header="Map" Binding="{Binding MapId}" Width="45"/>
                    <DataGridTextColumn Header="Position" Binding="{Binding Position}" Width="130"/>
                    <DataGridTextColumn Header="Screen" Binding="{Binding ScreenState}" Width="80"/>
                    <DataGridTextColumn Header="State" Binding="{Binding ConnectionState}" Width="90"/>
                    <DataGridTextColumn Header="Action" Binding="{Binding CurrentAction}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Row 2: Status -->
        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="DarkSlateGray" Margin="0,6,0,0"/>
    </Grid>
</UserControl>
