<Window x:Class="WoWStateManagerUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:WoWStateManagerUI.Views"
        xmlns:vm="clr-namespace:WoWStateManagerUI.ViewModels"
        xmlns:convert="clr-namespace:WoWStateManagerUI.Converters"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        mc:Ignorable="d"
        Title="Westworld of Warcraft — State Manager" Width="1100" Height="650" FontSize="11">
    <Window.Resources>
        <convert:ServiceStatusToBrushConverter x:Key="StatusBrushConverter" />
    </Window.Resources>

    <DockPanel>
        <!-- Status bar at bottom: service health indicators -->
        <StatusBar DockPanel.Dock="Bottom" Padding="4,2">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="10" Height="10" Fill="{Binding HealthCheck.RealmdStatus, Converter={StaticResource StatusBrushConverter}}" Margin="0,0,4,0"/>
                    <TextBlock Text="Realmd" Margin="0,0,12,0"/>

                    <Ellipse Width="10" Height="10" Fill="{Binding HealthCheck.MangosdStatus, Converter={StaticResource StatusBrushConverter}}" Margin="0,0,4,0"/>
                    <TextBlock Text="Mangosd" Margin="0,0,12,0"/>

                    <Ellipse Width="10" Height="10" Fill="{Binding HealthCheck.SoapStatus, Converter={StaticResource StatusBrushConverter}}" Margin="0,0,4,0"/>
                    <TextBlock Text="SOAP" Margin="0,0,12,0"/>

                    <Ellipse Width="10" Height="10" Fill="{Binding HealthCheck.PathfindingStatus, Converter={StaticResource StatusBrushConverter}}" Margin="0,0,4,0"/>
                    <TextBlock Text="Pathfinding" Margin="0,0,12,0"/>

                    <Ellipse Width="10" Height="10" Fill="{Binding HealthCheck.SceneDataStatus, Converter={StaticResource StatusBrushConverter}}" Margin="0,0,4,0"/>
                    <TextBlock Text="SceneData" Margin="0,0,12,0"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>

        <!-- Main TabControl -->
        <TabControl Margin="4">
            <TabItem Header="Dashboard">
                <views:DashboardView DataContext="{Binding Dashboard}"/>
            </TabItem>
            <TabItem Header="MaNGOS Console">
                <views:MangosConsoleView DataContext="{Binding MangosConsole}"/>
            </TabItem>
            <TabItem Header="Accounts">
                <views:AccountManagementView DataContext="{Binding AccountManagement}"/>
            </TabItem>
            <TabItem Header="Config Editor">
                <views:ConfigEditorView DataContext="{Binding ConfigEditor}"/>
            </TabItem>
            <TabItem Header="Instances">
                <views:InstancesView DataContext="{Binding Instances}"/>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
