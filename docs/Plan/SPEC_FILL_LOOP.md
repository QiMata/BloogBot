# Spec-Fill Loop Tracker

> One pass per row. Mark `done` when (a) the spec is implementation-ready,
> (b) ML hooks are spec'd where they fit, (c) contract tests are written
> or stubbed, (d) the commit is pushed.

| # | File | Owner type | ML hook | Status | Outcome |
|---|------|------------|---------|--------|---------|
| 1 | [docs/Spec/19_AOTA_RUNTIME.md](../Spec/19_AOTA_RUNTIME.md) | Spec | Composer-tiebreaker advice | done | Added proto fields 33-37 (current_activity_id/_objective_id/_objective_type/advice_log/last_objective_failure), FailureReason enum, ObjectiveContext feature shape, three-phase ML maturity, §10 dynamic-progressive invariant; 4 Skip-stubbed contract tests in IActivityContractTests.cs. |
| 2 | [docs/Spec/20_DECISION_ENGINE.md](../Spec/20_DECISION_ENGINE.md) | Spec | This IS the ML surface | done | Added §2.1 full proto wire shape (AdviceRequest/Response + 4 Context + 4 Advice + NoAdvice + AdviceError + AdvisorMode) with normative field numbers; §4.1 Config/decision-engine.json schema with hot-reload semantics; §4.2 ONNX feature tensor shapes per advisor; §6.1 JSONL trace schema + §6.2 correctness contract; §10 dynamic-progressive invariant; §11 Plan/14 slot table; 5 Skip-stubbed contract tests in DecisionEngineClientTests.cs incl. dynamic-progressive. Also fixed Spec/19 slot refs to point at S10.2/S10.5/S10.6/S10.7 (not S10.3) and replaced its duplicate FailureReason enum with a mapping table to Spec/12. |
| 3 | [docs/Spec/21_SOCIAL_FABRIC.md](../Spec/21_SOCIAL_FABRIC.md) | Spec | Chat-template selection | done | Deepened §3.3 generator to ChatPostPlan + 3-phase template-picker advisor; §9 proto fields 38-39 (chat_post_budgets + pending_mail_count); §10 failure-reason mapping incl. 3 new Spec/12 follow-ups (mail_recipient_invalid, chat_denylist_rejection, social_channel_join_failed); §11 ML integration consumes new Spec/20 GetChatTemplateAdviceAsync; §12 dynamic-progressive invariant (≥10% wts conversion); §13 Plan/15 slot table. Spec/20 extended: added 5th advisor (RPC + ChatTemplateContext/Advice proto messages with oneof tag 5/6 + ONNX [1,48] row + budgetMs=20 config row). 8 Skip-stubbed tests in SocialFabricContractTests.cs incl. dynamic-progressive. |
| 4 | [docs/Spec/22_WORLD_CYCLES.md](../Spec/22_WORLD_CYCLES.md) | Spec | World-buff-window forecasting | done | §9 proto fields 40-42 (world_buff_windows, current_lockouts, active_game_events) with WorldBuffWindowProj/LockoutStateProj/ActiveGameEventProj message shapes; §10 failure-reason mapping incl. new follow-up world_buff_window_missed; §11 ML integration reuses GetObjectiveAdviceAsync as buff-window pivot tiebreaker (no new advisor — extends Spec/20 ObjectiveContext's roster_goal_distance with a raid-readiness deficit axis computed locally); §12 dynamic-progressive invariant; §13 Plan-slot table flags 4 orphan services (GameEventProvider, WorldBossWatcher, WorldBuffWindowTracker, CalendarResetService) as Plan follow-ups. 6 Skip-stubbed tests incl. dynamic-progressive. |
| 5 | [docs/Spec/23_ONDEMAND_API.md](../Spec/23_ONDEMAND_API.md) | Spec | Request-disambiguation NLP | done | Added RATE_LIMITED + AMBIGUOUS_REQUEST rejection codes; §10 proto fields 43-44 (ondemand_instances[OnDemandInstanceProj] + recent_ondemand_echoes[OnDemandRequestEcho]); §11 failure-reason mapping incl. new follow-up ondemand_stage_timeout; §12 ML integration consumes new Spec/20 GetActivityRequestAdviceAsync (6th advisor) with explicit AMBIGUOUS_REQUEST fall-soft path; §13 dynamic-progressive invariant (same whisper, different operator contexts -> resolution may diverge AND closes aggregate roster distance); §14 Plan-slot table covering S2.5/S2.6/S2.7/S10.5/S10.6 + Plan/04 UI. Spec/20 extended: ActivityRequestContext/Advice messages (oneof tags 6/7), ONNX [1, 72] row, budgetMs=30. 9 Skip-stubbed tests incl. dynamic-progressive. |
| 6 | [docs/Spec/24_BEHAVIORAL_VARIATION.md](../Spec/24_BEHAVIORAL_VARIATION.md) | Spec | Personality-clustering | done | §9 proto field 45 (PersonalityProj with personality_hash + cluster_id + ChattyLevel + RewardPriority + flags); §10 failure-reason mapping (no new Spec/12 values; personality fails gracefully into Deterministic fallback); §11 ML integration consumes new Spec/20 GetPersonalityClusterAdviceAsync (7th advisor) at one-shot profile-generation time, biases knob sampling via cluster centroids; §12 dynamic-progressive invariant (per-tick timing diverges, outcomes stay stable within 1.5x baseline); §13 Plan/16 slot table covering S12.1-S12.11 + Plan/14 S10.6. Spec/20 extended: 7th advisor RPC + PersonalityClusterContext/Advice messages (oneof tags 7/8), ONNX [1, 40] row, budgetMs=500. 8 Skip-stubbed tests incl. dynamic-progressive. |
| 7 | [docs/Plan/13_PHASE9_CATALOG_FILL.md](13_PHASE9_CATALOG_FILL.md) | Plan | Catalog-row auto-suggest | done | All 9 slots S9.1-S9.9 deepened with read-only paths, spec contracts, procedure steps, success criteria, failure modes, ML sub-bullets. Added Dynamic-progressive invariant section (expanded catalog must produce diverging Activity selections for distinct bots AND every new row must close roster_distance_delta in ≥1 trace), ML integration umbrella (off-line tools only, no new runtime advisor — consumes Spec/20 §6 traces for catalog-row auto-suggest + holiday prioritization), Plan-slot cross-reference table. 9 Skip-stubbed tests in Phase9CatalogContractTests.cs incl. Phase9CatalogFill_DynamicProgressive_ExpandedCatalogClosesGoalDistance. |
| 8 | [docs/Plan/14_PHASE10_DECISION_ENGINE_INTEGRATION.md](14_PHASE10_DECISION_ENGINE_INTEGRATION.md) | Plan | Wires every ML hook | done | Exit criteria + S10.0 updated from 4-advisor to 7-advisor surface (chat_template + activity_request + personality_cluster added by passes 3/5/6 now reflected). All 9 prior slots S10.0-S10.8 deepened with read-only paths, spec contracts, procedure steps, success criteria, failure modes, ML sub-bullets. Three new slots S10.9 (chat_template wire, owns TemplateChatGenerator), S10.10 (activity_request wire, owns OnDemandActivitiesModeHandler advice consumer), S10.11 (personality_cluster wire, owns PersonalityFactory advice consumer). S10.5 AdviceLog buffer cap corrected from 20 to 8 to match Spec/19. S10.8 LiveValidation expanded from 2 to 7 tests (one per wire path + DecisionEngine_DynamicProgressive_AllAdvisorsForcedToNoAdvice canary). Dynamic-progressive invariant section + Plan-slot cross-reference table to Spec/20 §1-§11. 10 Skip-stubbed tests in Phase10DecisionEngineContractTests.cs. |
| 9 | [docs/Plan/15_PHASE11_SOCIAL_FABRIC.md](15_PHASE11_SOCIAL_FABRIC.md) | Plan | Whisper-intent classifier | done | All 13 slots S11.1-S11.13 deepened with read-only paths, spec contracts (cited verbatim with section anchors), procedure steps, success criteria, failure modes, per-slot ML sub-bullets. S11.5/S11.6/S11.10 explicitly identified as Plan/14 S10.9 advisor consumers. S11.10 whisper-intent classifier extended to a Phase 1/2/3 maturity ladder that fits inside the existing Spec/20 §2.2 chat_template advisor via extended ChatTemplateContext.trigger_kind granularity (no new RPC). Added Dynamic-progressive invariant (≥10% wts→trade conversion + ≥5% lfg→invite) + ML integration umbrella + Plan-slot cross-reference. 10 Skip-stubbed tests in Phase11SocialFabricContractTests.cs incl. Phase11SocialFabric_DynamicProgressive_TradeChatBuyersConvert. |
| 10 | [docs/Plan/16_PHASE12_BEHAVIORAL_VARIATION.md](16_PHASE12_BEHAVIORAL_VARIATION.md) | Plan | Mix-distribution learner | done | All 11 slots S12.1-S12.11 deepened with read-only paths, spec contracts (anchored), procedure, success criteria, failure modes, ML sub-bullets. S12.1 explicitly wires Plan/14 S10.11 personality_cluster advisor at profile-generation time (one-shot, 500ms budget, fail-soft to uniform mix). S12.10 corrected: field 45 PersonalityProj on WoWActivitySnapshot root, NOT Player.PersonalityHash (Spec/24 §9 authoritative). S12.8 ML sub-bullet describes the off-line mix-distribution learner (Gaussian mixture over real-player traces, produces PR for Config/personalities.json + cluster centroid file for Plan/14 S10.11). Exit criteria gained a personality_cluster advisor wiring requirement. Two umbrella sections: Dynamic-progressive invariant + ML integration umbrella + Plan-slot cross-reference. 11 Skip-stubbed tests in Phase12BehavioralVariationContractTests.cs. |
| 11 | [docs/Spec/05_PROGRESSION.md](../Spec/05_PROGRESSION.md) | Spec (existing) | RosterPlanner objective scoring | done | Grounded the `RosterPlanner.Distance(snapshot, goal)` metric that the entire loop's dynamic-progressive invariant refers to: explicit `RosterPlannerDistance` record + 8-axis `DistanceAxis` enum + `DefaultWeights` summing to 1.0 + pure-function contract (no I/O / no clock). Snapshot proto field 46 `RosterPlannerDistanceProj` (matches Spec/20 §2.1 ObjectiveContext.roster_goal_distance[8]). Failure-reason mapping (no new Spec/12 values). ML integration consumes Plan/14 S10.2 (objective tie-breaker via GetObjectiveAdviceAsync) only when expected-delta ties within 1e-3; lowest-id-lex fallback. Dynamic-progressive invariant grounded with explicit formula. Plan-slot cross-reference flags two orphan services (RosterPlanner + ProgressionPlanner) as Plan follow-ups. 6 Skip-stubbed tests in ProgressionContractTests.cs incl. Progression_DynamicProgressive_DistanceStrictlyDecreasesPerActivity. |
| 12 | [docs/Spec/04_ACTIVITIES.md](../Spec/04_ACTIVITIES.md) | Spec (existing) | BotSelectionPolicy weights | done | Grounded BotSelectionScorer.Score(bot, activity) formula with explicit per-weight contribution (RoleFit + LevelFit + Interruptibility + TravelEta + GearReadiness + ClassUtility + ProgressionOpportunity + RecentFailurePenalty + HumanPreference) + tie-break rules (lowest RecentFailureCount → lex-lowest AccountName); added per-activity `classUtility` JSON shape; failure-reason mapping (no new Spec/12 values); snapshot projection extends Spec/23 OnDemandInstanceProj field 43 with `selection_results[BotSelectionEntry]` (cap top 8 by score); ML integration is off-line weight-learner (gradient-boosted regressor over outcome traces; output is `Config/activities/<id>.json` PR) — no new Spec/20 RPC since selection is ≤1/min and per-launch advisor budget doesn't fit; dynamic-progressive invariant (different pool states → different rosters AND `roster_distance_delta_aggregate ≤ 0`); Plan-slot cross-reference. 7 Skip-stubbed tests in BotSelectionContractTests.cs incl. Activities_DynamicProgressive_BotSelectionRespectsProgressionCost. |
| 13 | [docs/Spec/03_BOTRUNNER.md](../Spec/03_BOTRUNNER.md) | Spec (existing) | IRewardSelector phase-3 ML | done | §Reward selection deepened from prose phase list to implementation-ready: three maturity phases mapped to AdvisorMode (Trivial/Rules/Ml) per Spec/20 §5; explicit RewardSelector.SelectQuestRewardAsync code skeleton showing the GetRewardAdviceAsync wire, confidence floor, AdviceLogEntry emission, fall-soft to Phase-1 trivial; RewardContext field-by-field build table including the IMangosCatalog read; tests-by-name section listing 6 contract tests; snapshot projection uses existing advice_log field 36 (no new proto field needed); Plan-slot cross-reference covering S10.1/S10.6/S10.7/S10.8 + S12.7 RewardPriority bias + S2.9 trivial selector. Replaced the stale "Plan/10 future skill" reference with concrete Plan/14 slots. 6 Skip-stubbed tests in RewardSelectorContractTests.cs incl. BotRunner_DynamicProgressive_RewardSelectionClosesBisDistance. |
| 14 | [docs/Spec/10_METRICS.md](../Spec/10_METRICS.md) | Spec (existing) | Anomaly-detection metric | done | Added §Anomaly detection section: 4 new metrics (anomaly.detected_total + active gauge + suppressed_total + resolved_total), AnomalyKind enum (16 closed-set values incl. RosterDistanceRegression as the canary for the loop's dynamic-progressive invariant), AnomalySeverity (Info/Warning/Critical) + AnomalySource (RuleEngine/OnnxModel), Anomaly event message + snapshot field 47 active_anomalies[] (ring buffer cap 16). ML integration framed as OBSERVATIONAL (not decision) — does NOT extend Spec/20 advisor RPCs; instead a service-internal IAnomalyDetector worker inside DecisionEngineService consumes Spec/20 §6 trace stream + metric scrapes + emits Anomaly events. Three-phase ladder (static thresholds / learned thresholds / ONNX isolation-forest). Failure-reason mapping (no new Spec/12 values). Dynamic-progressive invariant: detector MUST fire on outcome.roster_distance_delta > 0 AND MUST NOT fire on healthy traces. Plan-slot cross-reference flags AnomalyDetector as a Plan follow-up (7th orphan). 7 Skip-stubbed tests in AnomalyDetectionContractTests.cs incl. Anomaly_DynamicProgressive_HealthyTraceClearMatchesRegressionTraceTriggers. |
| 15 | [docs/Spec/12_ERROR_TAXONOMY.md](../Spec/12_ERROR_TAXONOMY.md) | Spec (existing) | Failure-cause clustering | done | Added 6 new FailureReason values harvested from pass-2/3/4/5 follow-ups: objective_end_state_unreachable (Spec/19 §8), mail_recipient_invalid + chat_denylist_rejection + social_channel_join_failed (Spec/21 §10), world_buff_window_missed (Spec/22 §10), ondemand_stage_timeout (Spec/23 §11). Enum file updated in lockstep with doc; existing FailureReasonCatalogTests drift suite passes (2/2). Added §Per-reason mapping table with detail-string convention per reason. Added §Failure-cause clustering ML integration: OBSERVATIONAL ML (does NOT extend Spec/20 advisor RPCs), three-phase ladder (trigram-hash buckets / config-rule patterns / ONNX embedding clusters); emits ActivityFailureCluster on Spec/10's active_anomalies field 47. Failure-cause clustering threshold (5 members / 10 min window) backpressures 47 identical failures into 1 cluster event. Dynamic-progressive invariant: FailureReason MUST be absent on progressive outcomes (failure + progress mutually exclusive). Plan-slot cross-reference flags FailureClusterer.cs as 8th orphan service. 7 Skip-stubbed tests in FailureClusteringContractTests.cs. The 7th deferred value (objective_decision_engine_rejected) remains deferred per pass-2 note — only relevant if Phase-3 ML returns a hard veto. |
| 16 | [docs/Spec/13_TESTING.md](../Spec/13_TESTING.md) | Spec (existing) | Training-trace capture | done | Added §Training-trace capture: every LiveValidation test produces a JSONL trace under tmp/test-runtime/traces/<TestClass.TestMethod>/<timestamp>.jsonl per Spec/20 §6.1; mandatory for LiveValidation; off for unit/contract/component; [NoTrainingTrace("reason")] attribute as the opt-out with human-readable reason captured in fixture teardown. WoWStateManagerUIFixture owns lifecycle. Trace writer subscribes to snapshot stream / advice request-response / IActivity.NextObjective transitions / IBotTask terminal callbacks / IActivity.OnComplete; correctness contract from Spec/20 §6.2 enforced at fixture teardown as test-failure (corrupt evidence). PII safeguards: chat strings hashed, no foreign account names, positions rounded to 1m precision per Spec/10 cardinality budget. §Failure-reason mapping (no new Spec/12 values). §ML integration umbrella positions this spec as the PRODUCER for the entire Spec/20 §6 pipeline that all seven Phase-3 advisors train against. §Dynamic-progressive invariant: every outcome line with completion="complete" MUST have roster_distance_delta ≤ 0 (cosmetic-only delta=0 allowed; strictly-positive is a regression). 6 Skip-stubbed tests in TrainingTraceCaptureContractTests.cs incl. Testing_DynamicProgressive_LiveValidationProducesNonPositiveRosterDelta. Plan-slot cross-reference flags WoWStateManagerUIFixture as 9th orphan service. |
| 17 | [docs/Spec/15_SKILLS.md](../Spec/15_SKILLS.md) | Spec (existing) | Cross-game-skill auto-bootstrap | done | Added §Skill applicability scoring: off-line auto-bootstrap tool consumes skill descriptors + target CLAUDE.md/AGENTS.md + open TASKS.md + Spec/20 §6 trace history; outputs SkillApplicability {skill_name, score [0..1], rationale, suggested_task_ids[]} per skill. §Trace capture for skills writes JSONL under tmp/test-runtime/traces/Skill_<name>_*/ with a skill-specific outcome shape (TASKS.md items closed + roster_distance_delta_cross_game). §Failure-reason mapping (no new Spec/12 values). §ML integration is OFF-LINE tooling (mirrors Spec/04 + Spec/10 + Spec/12 pattern); three phases: trigger-regex / per-game rule-table / sentence-transformer ONNX embedding-similarity. Self-target calibration canary asserts WWoW skills score >=0.95 against WWoW. §Dynamic-progressive invariant (different target repos -> different rankings AND each dispatched skill closes >=1 task item). §Plan-slot cross-reference covers Plan/11 + Plan/14 S10.7 + Plan/13 S9.8 test pattern; SkillAutoBootstrap flagged as 10th orphan service. 6 Skip-stubbed tests in SkillsContractTests.cs incl. Skills_DynamicProgressive_DispatchedSkillClosesTaskItem. Build clean. |
| 18 | [docs/architecture/aota/03_DYNAMIC_COMPOSITION.md](../architecture/aota/03_DYNAMIC_COMPOSITION.md) | Arch | Composer learning loop | done | Appended §9 ML-aided composition (ASCII diagram of the composer learning loop showing detected-tie → advisor → trace → off-line training → ONNX → Mode flip back to runtime; preserves the §3 deterministic algorithm contract — advisor only chooses among already-valid orderings, cannot invent Objectives). §10 worked example "level 32 Westfall-exhausted Alliance Paladin → STV vs Hillsbrad transition" walks through all three phases (lex-tie / rules-table / ONNX picking Hillsbrad based on +0.04 roster_distance_delta/hr from prior trace history). §11 dynamic-progressive invariant on the composer side: ML accelerates closure but cannot reverse it; advisor only picks among tied options, all of which produce non-positive delta by §3 step 6 construction. §12 Plan-slot cross-reference flags two new orphan services (RosterPlanner.cs from Spec/05 already logged + ComposerOutcomeRecorder.cs that writes outcome lines from ActivityCompletion). 7 Skip-stubbed tests in DynamicCompositionContractTests.cs covering tie-detection-within-epsilon / NoAdvice fallback / out-of-set ignored / low-confidence ignored / Rules-file-missing fallback / Ml-aided-pick-still-in-tied-set / AotaDynamicComposition_LearningLoop_AdviceRespectsTieSetAndProgresses. Build clean. |
| 19 | [docs/architecture/aota/04_QUEST_CHAINS.md](../architecture/aota/04_QUEST_CHAINS.md) | Arch | Quest-chain ordering optimizer | done | Appended §11 quest-chain ordering optimizer (re-uses GetObjectiveAdviceAsync — no new RPC; tied_objective_ids carries concurrently-eligible chain heads instead of composer ties). Three maturity phases: Phase 1 lowest-travel + highest-XP heuristic, Phase 2 Bot/quests/zone-<name>.json preferredInterleave hand-authored hints, Phase 3 ONNX trained on Quest_*/outcome traces; same objective/v1.onnx model file extended with quest-chain features. §12 worked example extends §9 Defias chain with 2 side quests + cross-zone Redridge expedition: Phase 1 picks coyote-cull first (closest), Phase 2 with preferredInterleave injects coyote-cull after turnin-132, Phase 3 picks differently based on roster_goal_distance[GoldTargetPct] axis dominance (gold-near-goal → coyote first for pelts vendor; gold-far-from-goal → Defias chain first). §13 dynamic-progressive invariant (axis dominance flips chain-head pick AND both chains close RosterPlanner.Distance). §14 Plan-slot cross-reference flags QuestChainHeuristic.cs as 12th orphan service. 4 Skip-stubbed tests in QuestChainOrderingContractTests.cs incl. AotaQuestChains_OptimizerProducesDifferentOrderPerRosterAxisDominance. Build clean. |
| 20 | [docs/architecture/aota/05_ITEM_REQUIREMENTS.md](../architecture/aota/05_ITEM_REQUIREMENTS.md) | Arch | Cheapest-source learner | done | Appended §9 cheapest-source learner reusing GetObjectiveAdviceAsync — no new RPC; tied_objective_ids carries per-source `acquire-from-<kind>-<entry|id>` candidates from the §4 provenance walk. Three phases: heuristic minBy(cost), per-(itemId, level_band, faction) Config/decision-engine/item-source-rules.json overrides, ONNX trained on Item_*/outcome traces with explicit features (source_kind one-hot, drop-rate variance, AH listing age, craft recursion depth, historical actual-cost P50/P90, bot gold relative to target). §10 worked example: Drakefire Amulet (single-source — Phase-2 rule is a guardrail for misconfig); plus Greater Healing Potion sub-example showing Phase-3 picks CRAFT vs DROP vs AH based on roster_goal_distance axis dominance (ProfessionSkill-dominant prefers CRAFT for Sungrass herbalism side-effect; Level-dominant prefers DROP for EPL XP grind side-effect). §11 dynamic-progressive invariant (different axis dominance → different source picks; ML cannot invent sources outside §4 provenance DAG, cannot pick BoP-from-AH). §12 Plan-slot cross-reference flags ItemSourceResolver.cs + MarketDataCache.cs as 13th + 14th orphan services. 4 Skip-stubbed tests in ItemRequirementsContractTests.cs incl. AotaItemRequirements_LearnerPicksDifferentSourcePerAxisDominance. Build clean. |
| 21 | [docs/architecture/aota/06_WORKED_EXAMPLES.md](../architecture/aota/06_WORKED_EXAMPLES.md) | Arch | Add ML-aided example | done | Added §Example 5 "L40 Gnome Frost Mage zone transition" threading SIX advisor consultations through one Activity (Activity selection / quest-chain ordering / cheapest-source learner / sub-Objective tie-break / reward selector / one-shot personality cluster). Each consultation shows: ObjectiveContext sent, Phase-1 fallback pick, Phase-3 ML pick with rationale, resulting trace line. The bot's AttunementStep-dominant axis tips Consultation 1 toward reputation.argent-dawn over dungeon.stratholme-undead lex-fallback; subsequent consultations chain-optimize WPL quest interleave (5805 bone-fragment-stack opportunism first), reject stale AH listing in favor of Wandering-Skeleton drop with empirically-shorter actual_cost_p50, and pick BiS chest over BiS head reward by current-gear axis distance. Outcome: 2h15min vs 3h heuristic (saves 45min); roster_distance_delta=-0.087 vs -0.061 deterministic-replay (both progressive — ML accelerates closure but cannot reverse it). Extended Cross-example takeaway item 5 (ML accelerates, never reverses; the deterministic stack is the floor, ML is the ceiling not a wall). Added §Plan-slot cross-reference + §Test surface. 3 Skip-stubbed tests in MlAidedWorkedExampleContractTests.cs incl. MlAidedWorkedExample_DynamicProgressive_OutcomeDeltaIsNonPositive. Build clean. |
| 22 | [docs/architecture/aota/07_PORTABILITY.md](../architecture/aota/07_PORTABILITY.md) | Arch | Cross-game ML reuse note | done | Added §7 Cross-game ML reuse with per-advisor portability matrix: interface shape portable; trained .onnx files NOT portable except partially for the `objective` advisor (8-axis roster_goal_distance is universal) and for the `personality_cluster` advisor (chattiness + axis distributions travel). Trace JSONL schema fully portable; trainer toolchain fully portable; per-game rules/lookup tables NOT portable. The Spec/15 skill-applicability scorer is the ONE genuinely cross-game ML model (its labeled data spans games). §8 worked example: porting WWoW Objective advisor to FFXI in 5 steps (port interface; port `RosterPlanner.Distance` 8-axis mapping with FFXI-specific axis names like "key-items-and-missions"/"fame-tiers"/"chocobo-license"/"ballista-rank"; copy trace schema; warm-start with WWoW v1.onnx at confidence_threshold=0.7; re-train v2 after 50 FFXI native traces). §9 dynamic-progressive invariant survives cross-game port unchanged (defined on the game-agnostic trace surface). §10 Plan-slot cross-reference flags Tools/CrossGameModelWarmStart/ as 15th orphan service. 5 Skip-stubbed tests in CrossGameMlReuseContractTests.cs incl. AotaPortability_DynamicProgressive_InvariantSurvivesCrossGamePort. Build clean. |
| 23 | [docs/Plan/Activities/00_INDEX.md](Activities/00_INDEX.md) | Plan-detail | Per-row training-trace plan | done | Added §Phase 9 expansion forward-reference (Plan/13 S9.1-S9.7 row sketches: SM 4-wing + Stockades + dungeon-quest subs + 10+ escorts + 6 holiday events + 6 social services + 2 WPvP gated; CatalogMarkdownDriftTests loosened to [130,180] per S9.8). §Training-trace contract per family: 15-row table (12 existing families + 3 forward-referenced Plan/13 families) mapping each to its Spec/20 advisors triggered, expected outcome-line count, and training-data value. Per-advisor corpus mapping: objective trains primarily on Zone Q + Dungeons + Raids + Reps + Attunements; rotation on Dungeons+Raids combat ticks; threat tank-only; reward on every turn-in; chat_template on Social+Economy; activity_request on Shodan whisper handler; personality_cluster on per-bot account-creation snapshots aggregated. §Dynamic-progressive invariant elevated to per-row contract: every row's trace MUST close roster_distance_delta < 0 at least once across a representative sample; rows that produce only 0-deltas are flagged DECORATION and must be justified. §Plan-slot cross-reference + §Test surface. 4 Skip-stubbed tests in IndexTrainingTracePlanContractTests.cs incl. Activities00Index_DynamicProgressive_NonPositiveRosterDeltaPerRow. Build clean. |

## Loop guard

- Operating contract: see the `/loop` prompt in the session that owns this tracker.
- ML surface authority: [docs/Spec/20_DECISION_ENGINE.md](../Spec/20_DECISION_ENGINE.md). Other specs MUST consume one of the four advisory RPCs there or extend that surface (with a corresponding Plan/14 slot).
- Dynamic-and-progressive invariant (per pass): every spec touched ships at least one contract test named `<SpecOrPlanShortName>_DynamicProgressive_<Property>Test` that asserts the composed Objective list varies with snapshot inputs AND that each Activity assignment closes distance to `CharacterRosterGoal` (Spec/05).
- Each advisor enumerates Phase 1 heuristic / Phase 2 rules+lookup / Phase 3 ONNX even if only Phase 1 ships today.

## Follow-ups (added in-pass)

<!-- Append discovered work as bullets. Do not delete completed entries; strike them with `~~text~~`. -->

- **(pass 2)** Spec/12 `FailureReason` may need two additions once S2.0 runtime ships: `objective_end_state_unreachable` (distinct from `task_unrecoverable` if composer-vs-runtime disagreement is a metric we want) and `objective_decision_engine_rejected` (only matters if Phase-3 ML ever returns a hard veto). Defer until live failure modes are observed; row 15 (Spec/12) pass should re-evaluate.
- **(pass 3)** Spec/12 `FailureReason` needs three new social-fabric values: `mail_recipient_invalid` (Plan/15 S11.4 references it explicitly; not in Spec/12 today), `chat_denylist_rejection` (Plan/15 S11.12 denylist trip), `social_channel_join_failed` (Plan/15 channel-join recovery). Row 15 (Spec/12) pass MUST add these; do not block Plan/15 implementation work on the row-15 pass.
- **(pass 4)** Four world-cycle services have no Plan slot: `GameEventProvider`, `WorldBossWatcher`, `WorldBuffWindowTracker`, `CalendarResetService`. Likely homes are a new Plan/17 phase OR sub-slots inside Plan/13 (Phase 9: Catalog completeness) since event-row presence/absence drives Activity eligibility. Add when the row-7 (Plan/13) pass runs OR consider opening Plan/17 explicitly. Also: Spec/12 may need `world_buff_window_missed` to distinguish from `task_timeout` if buff-pivot metric granularity becomes valuable.
- **(pass 5)** Spec/12 `FailureReason` needs one more value: `ondemand_stage_timeout` (Spec/23 §4 references `FailureReason.STAGE_TIMEOUT` on the proto stream event — the launcher's per-stage timeout is more specific than the bare `task_timeout` and the metric label is more useful split). Row 15 (Spec/12) pass MUST add it.
- **(pass 11)** Two progression services have no Plan slot: `Services/WoWStateManager/Progression/RosterPlanner.cs` (new, owns `Distance(...)` per Spec/05) and `Services/WoWStateManager/Progression/ProgressionPlanner.cs` (referenced as existing in Spec/05 §Existing code anchors but not yet owned by any slot). Likely homes: a new Plan/18 phase OR sub-slots inside Plan/03 (Phase 2 OnDemand). Add when the orphan-services Plan phase opens (combined with the pass-4 world-cycle orphans: GameEventProvider, WorldBossWatcher, WorldBuffWindowTracker, CalendarResetService → six orphan services total).
