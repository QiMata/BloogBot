namespace GameData.Core.Models.Activities
{
    /// <summary>
    /// The role a human player takes when joining an OnDemand activity.
    /// </summary>
    public enum HumanGroupRole
    {
        Member,
        Leader,
        Observer,
    }
}
